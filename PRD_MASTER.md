# PukedMaster 综合产品需求文档 (Master PRD)

## 1. 项目愿景 (The Vision)
**Puked (吐槽)** 是一个专为自动驾驶行业设计的**乘坐舒适度量化与标定平台**。
通过手机传感器捕捉自动驾驶车辆在行驶过程中的物理表现，结合乘坐者的主观负体验标记，建立“物理客观数据”与“人类主观感受”之间的映射关系。

---

## 2. 核心架构
### 2.1 模块关系
- **Native App (Puked)**: 采集终端。负责 100Hz 数据采集、重力校准、实时事件捕捉（Retroactive Tagging）及数据同步。
- **Web App (Puked Admin)**: 分析终端。负责多维度数据可视化、车队舒适度横向对比、算法阈值验证及数据导出。
- **Backend (PocketBase)**: 数据枢纽。存储行程、事件、用户信息及元数据。

---

## 3. Native App (终端采集 - 已实现/优化中)
### 3.1 传感器引擎
- **稳定采样**: 30Hz (加速度、陀螺仪)，兼顾功耗与精度，支持 100Hz 动态扩展。
- **坐标对齐**: 静态重力对齐算法，确保数据不受手机放置角度影响。
- **缓冲区**: 15 秒循环缓冲区，支持“回溯式”捕捉。

### 3.2 负体验捕捉 (Negative Experience Capture)
- **手动回溯 (Retroactive)**: 用户点击“吐槽”时，截取 T-10s 到 T+5s 的原始数据 (共计约 450 帧数据)。
- **自动识别**: 基于 G-force 阈值的急加减速、横向摆动、颠簸检测。

### 3.3 数据同步
- **摘要上传**: 行程结束后，自动提取 Metrics（里程、时长、事件统计）同步至云端。
- **轨迹降采样**: 为了渲染性能，轨迹点降采样至 50 点用于快照展示。

---

## 4. Web App (管理与分析 - 待实现)
### 4.1 核心需求 (First Principles)
1. **数据深度复盘**: 浏览器端展示高采样率波形图，支持逐帧查看事件发生瞬间的车辆姿态。
2. **多车型基准测试 (Benchmarking)**: 对比不同品牌 (Tesla, Huawei, NIO等) 或不同软件版本的舒适度得分。
3. **数据管理与导出**: 支持大规模 JSON/CSV 导出，用于算法团队的离线仿真。

### 4.2 功能模块规划
#### A. 行程看板 (Fleet Dashboard)
- **统计图表**: 事件分布饼图（急刹、颠簸、摆动占比）。
- **热力图**: 基于 GPS 的负体验事件地理分布。
- **活跃统计**: 不同测试员/车型的测试里程统计。

#### B. 行程分析器 (Trip Analyzer) - *Web 端核心*
- **联动播放器**: 
    - 左侧：交互式轨迹地图（基于 GCJ-02 纠偏）。
    - 右侧：同步波形图（纵向/横向加速度波形、角速度曲线）。
    - 底部：时间轴。拖动时间轴，地图点与波形点同步高亮。
- **事件切片**: 侧边栏列出该行程的所有负体验点，点击即跳转至对应的波形片段。

#### C. 竞技场管理 (Arena Management)
- **排行榜**: 管理和查看“竞技场”模式下的打分排名。
- **对决复盘**: 同一路径下，不同车型的波形叠加对比功能。

#### D. 系统设置
- **阈值远程控制**: 设置并下发“自动识别”的物理阈值（如 $0.3g$ 判定为急刹）。

---

## 5. 设计规范 (Design Standards)
- **风格**: Sophisticated Minimalism / Neo-Digitalism。
- **配色**: 
    - Background: `#0A0A0A` (深黑)
    - Primary: `#CCFF00` (荧光绿 - 智驾平顺)
    - Warning: `#FF3B30` (警示红 - 负体验)
- **技术选型建议**:
    - 前端: React / Next.js (图表库建议使用 ECharts 或 Canvas-based 库以处理高频数据)。
    - 状态管理: TanStack Query (配合 PocketBase SDK)。

---

## 6. 后续迭代
- **AI 自动分类**: 基于 15 秒原始波形，利用简单模型自动识别是“路面颠簸”还是“智驾规控不稳”。
- **视频联动**: 手机端同步录制前方视频，Web 端实现视频与波形联动。

